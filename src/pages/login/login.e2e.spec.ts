import { browser, element, by } from 'protractor';


var user;

describe('Login tests', () =>{
  beforeEach(() => {
    browser.get('');
    browser.driver.sleep(9500);
  });

  it('test', () =>{
    browser.driver.sleep(9500);
  });
});
